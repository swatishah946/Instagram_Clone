import 'package:flutter/material.dart';

class WebscreenLayout extends StatelessWidget {
  const WebscreenLayout({super.key});

  @override
  Widget build(BuildContext context) {
  return Scaffold(
      body: Center(child: const Text("this is web")),
    );
  }
}