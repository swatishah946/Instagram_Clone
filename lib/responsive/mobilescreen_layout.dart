import 'package:flutter/material.dart';

class MobilescreenLayout extends StatelessWidget {
  const MobilescreenLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: const Text("this is mobile")),
    );
  }
}